# Queries

query(
    collection(),
    filterBy(
        and(
            attributeEquals()
        )
    )
)

==>

Query(
    HeadConstraint,
    FilterConstraint(
        FilterConstraint(
            FilterConstraint
        )
    )
)