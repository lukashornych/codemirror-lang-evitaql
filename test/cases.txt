# Queries

query(
    collection("Product"),
    filterBy(
        and(
            attributeEquals()
        )
    )
)

==>

Query(
    HeadConstraint(String),
    FilterConstraint(
        FilterConstraint(
            FilterConstraint
        )
    )
)